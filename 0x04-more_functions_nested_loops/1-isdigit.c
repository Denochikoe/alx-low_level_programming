#include "main.h"
/**
 * _isdigit- checks for digit 0-9
 * c@: variable declaration
 * Return (1) or  (0)
 */
int _isdigit(int c)
{
	for (c >= 1; c <= 9; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
